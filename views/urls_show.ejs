
<!DOCTYPE html>
<html lang="en">
<head></head>
<body>
  <% include ./partials/_header.ejs %>
    <% for (var key in urls) { %>
    <%  if (key === shortURL) { %>
    <%= key %>:<%= urls[key].longURL %>
    <%  } %>
    <% } %>
    <p>
    <form method = "POST" action = "/urls/<%= shortURL%>">
      <input id="longURL" type="text" name="longURL" size="50" placeholder="<%= urls[shortURL].longURL %>">
      <input type="submit" value="Update">
    </form>
    </p>
</body>
</html>
